# Async School Management System

## Setup
1. Clone this repo
2. `cp .env.example .env`
3. `docker compose up -d`
4. Visit http://localhost:8069, create a database
5. Install the "School Management" module from Apps

## Structure
See addons/school_management/ — models split by domain (school_class, school_student, school_attendance, school_mark)